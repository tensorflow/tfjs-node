language: node_js
node_js: "8"
env:
  - CXX=g++-4.9

before_install:
  - sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y
  - sudo apt-get update -q
  - sudo apt-get install libc6 gcc-4.9 g++-4.9 -y

script:
  - yarn
  - yarn build
  - yarn lint
  - yarn test
