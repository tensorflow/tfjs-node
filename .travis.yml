language: node_js
node_js: "8"
env:
  - CXX=g++-4.9
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.9
      - gcc-4.9
      - libstdc++6
script:
  - yarn
  - yarn build
  - yarn lint
  - yarn test
